# js-ts-quality-checker-kit

## What is this?

This is a package that gets injected into your JS/TS project and takes over code quality checks using:

- Biome
- Knip
- Semgrep

You can invoke the tools explicitly via `npm run ...`, or hope that the SKILL.md instructions fire in your agent’s session.
